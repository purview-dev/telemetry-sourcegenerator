{
  Before:
using System.Diagnostics;

namespace Testing;

public class CacheService
{
	static readonly ActivitySource _activitySource = new("CacheService");

	public void FetchFromCache(string key)
	{
		using var activity = _activitySource.StartActivity("fetchFromCache");
	}
},
  After:
using System.Diagnostics;
using Purview.Telemetry;

namespace Testing;
[ActivitySource]
public interface ICacheServiceTracing
{
	[Activity]
	Activity? FetchFromCache();

}

public class CacheService
{
	static readonly ICacheServiceTracing _activitySource = new("CacheService");

	public void FetchFromCache(string key)
	{
		using var activity = _activitySource.FetchFromCache();
	}
}
}